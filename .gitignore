node_modules/
dist/
build/
coverage/
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/
prisma/*.db
uploads/

# Copias que hacen los scripts de entrega antes de tocar nada.
# Utiles en la maquina de quien aplica, pero NO en el repositorio: son
# versiones viejas de los mismos archivos, y alguna vez alguien va a abrir
# una creyendo que es la buena.
_respaldo_*/
_respaldos_bd/

# Respaldos que crea el script de aplicación de bloques antes de pisar
# archivos. Son una red de seguridad LOCAL: el historial de verdad es git,
# y meterlos dentro duplica el proyecto entero en cada entrega.
_respaldo-antes-*/
